akka {
  loglevel = INFO
}

gnafLuceneService {

  indexDir = "./indexDir"
  
  // validation limits
  bulk = 50
  numHits = 20
  minFuzzyLength = 2
  fuzzyMaxEdits = 2
  fuzzyPrefixLength = 0
  
  interface = "0.0.0.0"
  port = 9040
  
  indexDir = ${?GNAF_LUCENE_SERVICE_INDEX_DIR}
  
  numHits = ${?GNAF_LUCENE_SERVICE_NUM_HITS}
  minFuzzyLength = ${?GNAF_LUCENE_SERVICE_MIN_FUZZY_LENGTH}
  fuzzyMaxEdits = ${?GNAF_LUCENE_SERVICE_FUZZY_MAX_EDITS}
  fuzzyPrefixLength = ${?GNAF_LUCENE_SERVICE_FUZZY_PREFIX_LENGTH}
  
  interface = ${?GNAF_LUCENE_SERVICE_INTERFACE}
  port = ${?GNAF_LUCENE_SERVICE_PORT}
}

